node_modules/
dist/
.cache/

# Rendered video output. These are 4K MP4s — hundreds of MB each — and are
# build artifacts, not source. Deliver them out-of-band, don't commit them.
out/

# Claude Code scratch worktrees.
.claude/worktrees/

# Per-machine Claude Code permissions allowlist. Personal, not shared.
.claude/settings.local.json
